name: Open-Task

# Scripts de génération doc (écriture fichier depuis OpenRouter) — hors périmètre prod
paths-ignore:
  - scripts/docs

queries:
  - uses: security-and-quality
  - uses: security-extended
