{
  "name": "open-task-docs-scripts",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "description": "Pipeline doc-as-code : GitDiagram, chapitres IA, Prisma ERD, VitePress",
  "scripts": {
    "generate": "node cli.mjs generate",
    "assemble": "node cli.mjs assemble",
    "validate-mistral": "node cli.mjs validate-mistral",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "isomorphic-dompurify": "^2.30.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
